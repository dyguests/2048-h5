## 1. 工程与入口

- [ ] 1.1 初始化 H5 工程（如 Vite + 原生 TS/JS 或仓库约定栈），配置开发与构建脚本
- [ ] 1.2 添加游戏页入口 HTML、根容器与基础全局样式（安全区、禁止棋盘区误滚动 `touch-action`）

## 2. 核心逻辑与状态

- [ ] 2.1 实现 4×4 网格与纯函数式滑动、合并、空位检测与「是否可移动」判定
- [ ] 2.2 实现每次有效移动后的随机新块（2/4 概率按约定）与分数更新
- [ ] 2.3 将一次操作解析为「移动列表 / 合并事件」供动画层消费（含多格滑动路径信息）

## 3. 可爱风 UI

- [ ] 3.1 实现棋盘、方块圆角、柔和配色与数字层级样式（保证对比度与可读性）
- [ ] 3.2 展示当前分、最佳分（可 `localStorage`）、重开按钮与简单标题/装饰

## 4. 位移动画

- [ ] 4.1 为每个方块建立屏幕/网格坐标映射，滑动后用 transform（或等价）从旧格动画到新格，时长落在设计约定区间
- [ ] 4.2 多格连续移动在同一方向呈现连续位移；动画队列与下一次输入门禁：动画结算后再生成新块并解锁输入

## 5. 粒子与拖尾

- [ ] 5.1 在游戏区叠加粒子层（Canvas 或受限 DOM），在滑动/合并动画阶段触发闪耀粒子，`requestAnimationFrame` 更新，设粒子数量上限
- [ ] 5.2 为移动中的 tile 实现拖尾（轨迹点队列 + 淡出绘制或 CSS 等效），移动结束清理
- [ ] 5.3 合并瞬间在合并格附近增加一次粒子 burst，与移动粒子协调避免同帧过量

## 6. 输入与收尾

- [ ] 6.1 实现触摸滑动手势到四向映射；桌面方向键（或文档声明的快捷键）映射到相同逻辑
- [ ] 6.2 无效滑动不触发动画与新生成；可选轻量「无效」反馈
- [ ] 6.3 验证胜利/失败 UI 与重开流程；在目标设备上做一次性能与观感自测
